document.getElementById('guestsForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Implement your logic to handle the form data, e.g., saving it via AJAX.
    console.log("Form data saved");
});

function exportData() {
    // Implement your logic to export data, e.g., generating a CSV file.
    console.log("Data exported");
}